-- mod-playerbots-cata: optional migrations for the playerbots database.
-- Base schema is created from core files under data/sql/base/db_playerbots/.
-- This file registers the module update pipeline; it is safe to re-run (idempotent SELECT).

SELECT 1;
